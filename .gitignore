*build/

# The VS Code Arduino extension regenerates these with absolute paths to the
# machine that opened the sketch, so they are never portable.
.vscode/

# Editor and OS droppings.
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Never commit credentials.
.env
.env.*
*.pem
*.key
